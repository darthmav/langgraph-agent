"""Internal implementation module.

This module contains internal implementation details that should not be
imported directly by external consumers. Use the public API exposed via
the parent package's __init__.py instead.
"""
